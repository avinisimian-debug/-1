"use client";

import { LiveActivityToast } from "@/components/trust/LiveActivityToast";

/** Global social-proof toasts — landing + authenticated app. */
export function SocialProofLayer() {
  return <LiveActivityToast />;
}
